/*Write a JavaScript function that takes in two parameters, an array and a number(n), and return the first n elements of that array.*/

const arr = [2, 5, 7, 9, 10, 4, 3];
const n = 3;

function getArrCopy(arr, n) {
    
}